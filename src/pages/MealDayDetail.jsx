// const storedToken = localStorage.getItem("authToken"); add to axios call: { headers: { Authorization: `Bearer ${storedToken}` } }

import React from 'react';

export default function MealDayDetail() {
  return (
    <div>MealDayDetail</div>
  )
}
